package com.udihaguel.weatherapp.repository

import com.udihaguel.weatherapp.db.dao.WeatherDao
import com.udihaguel.weatherapp.model.WeatherData
import javax.inject.Inject

class RoomRepository @Inject constructor(){

    //fun updateLatestLocation(weather:WeatherData) = weatherDao.updateLatestLocation(weather)

    //fun getLatestLocation(id:String) = weatherDao.getLatestLocation(id)
}